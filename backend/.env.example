# Luméa Backend Server Configuration

PORT=5000

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lumea_db

# Note: If MySQL is not running or credentials are not supplied,
# the backend gracefully activates Fallback In-Memory Mode with seed data.
